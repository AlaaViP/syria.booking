import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div style={{padding: 24, fontFamily: 'system-ui', direction: 'rtl'}}>
    ✅ Hello from SGE — الاختبار يعمل. إذا ترى هذا السطر فالمشكلة داخل <App/> وليس في النشر.
  </div>
);
